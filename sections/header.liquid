{%- liquid
  assign s = section.settings
  assign logo_height = s.logo_height | default: settings.logo_height | default: 52
  assign logo_height_mobile = s.logo_height_mobile | default: settings.logo_height_mobile | default: 36
-%}

<header class="header" role="banner" {{ section.shopify_attributes }}>
  <div class="container header__inner">
    <a href="{{ routes.root_url }}" class="logo logo--img" aria-label="{{ shop.name | escape }} home">
      {%- if s.logo != blank -%}
        <img src="{{ s.logo | image_url: width: 600 }}"
             srcset="{{ s.logo | image_url: width: 300 }} 300w, {{ s.logo | image_url: width: 600 }} 600w"
             sizes="(max-width: 560px) {{ logo_height_mobile }}px, {{ logo_height }}px"
             alt="{{ s.logo.alt | default: shop.name | escape }}"
             loading="eager"
             width="{{ logo_height | times: 3 }}"
             height="{{ logo_height }}">
      {%- elsif settings.logo != blank -%}
        <img src="{{ settings.logo | image_url: width: 600 }}"
             srcset="{{ settings.logo | image_url: width: 300 }} 300w, {{ settings.logo | image_url: width: 600 }} 600w"
             sizes="(max-width: 560px) {{ logo_height_mobile }}px, {{ logo_height }}px"
             alt="{{ settings.logo.alt | default: shop.name | escape }}"
             loading="eager"
             width="{{ logo_height | times: 3 }}"
             height="{{ logo_height }}">
      {%- else -%}
        <span class="logo__text">{{ shop.name }}</span>
      {%- endif -%}
    </a>

    <nav class="nav" aria-label="Primary">
      {%- for block in section.blocks -%}
        {%- if block.type == 'menu' -%}
          {%- assign menu = block.settings.menu -%}
          {%- if menu != blank -%}
            {%- for link in menu.links -%}
              {%- if link.links.size > 0 -%}
                {%- assign drawer_id = 'nav-drawer-' | append: link.title | handleize | append: '-' | append: section.id -%}
                <button type="button"
                        class="nav__item nav__item--has-sub{% if block.settings.accent %} nav__item--accent{% endif %}"
                        data-nav-trigger="{{ drawer_id }}"
                        aria-expanded="false"
                        aria-controls="{{ drawer_id }}">
                  {{ link.title }}
                  <svg class="nav__chevron" viewBox="0 0 10 6" aria-hidden="true" focusable="false" width="10" height="6"><path d="M9.354.646a.5.5 0 00-.708 0L5 4.293 1.354.646a.5.5 0 00-.708.708l4 4a.5.5 0 00.708 0l4-4a.5.5 0 000-.708z" fill="currentColor"/></svg>
                </button>
              {%- else -%}
                <a href="{{ link.url }}" class="nav__item{% if block.settings.accent %} nav__item--accent{% endif %}"
                   {{ block.shopify_attributes }}>
                  {{ link.title }}
                </a>
              {%- endif -%}
            {%- endfor -%}
          {%- endif -%}
        {%- endif -%}
      {%- endfor -%}
    </nav>

    <div class="nav-drawers" data-nav-drawers>
      {%- for block in section.blocks -%}
        {%- if block.type == 'menu' -%}
          {%- assign menu = block.settings.menu -%}
          {%- if menu != blank -%}
            {%- for link in menu.links -%}
              {%- if link.links.size > 0 -%}
                {%- assign drawer_id = 'nav-drawer-' | append: link.title | handleize | append: '-' | append: section.id -%}
                <div id="{{ drawer_id }}"
                     class="nav-drawer"
                     data-nav-drawer
                     data-trigger="{{ drawer_id }}"
                     aria-hidden="true">
                  <div class="nav-drawer__backdrop" data-nav-drawer-close></div>
                  <aside class="nav-drawer__panel" role="dialog" aria-modal="true" aria-label="{{ link.title | escape }}">
                    <header class="nav-drawer__head">
                      <button type="button" class="nav-drawer__close" data-nav-drawer-close aria-label="Close menu">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" width="20" height="20" aria-hidden="true"><path d="M18 6L6 18M6 6l12 12"/></svg>
                      </button>
                      <span class="nav-drawer__title">{{ link.title | escape }}</span>
                    </header>
                    <nav class="nav-drawer__list" aria-label="{{ link.title | escape }}">
                      {%- for child_link in link.links -%}
                        <a href="{{ child_link.url }}" class="nav-drawer__item">{{ child_link.title | escape }}</a>
                      {%- endfor -%}
                    </nav>
                  </aside>
                </div>
              {%- endif -%}
            {%- endfor -%}
          {%- endif -%}
        {%- endif -%}
      {%- endfor -%}
    </div>

    <div class="header__actions">
      <button type="button" class="icon-btn menu-toggle" aria-label="Open menu" data-mobile-menu-toggle>
        {% render 'icon', icon: 'menu' %}
      </button>

      {%- if s.show_search -%}
        <button type="button" class="icon-btn" aria-label="Search" data-search-toggle>
          {% render 'icon', icon: 'search' %}
        </button>
      {%- endif -%}

      {%- if s.show_account -%}
        <a href="{{ routes.account_url }}" class="icon-btn" aria-label="Account">
          {% render 'icon', icon: 'user' %}
        </a>
      {%- endif -%}

      <a href="{{ routes.cart_url }}" class="cart-btn" aria-label="Cart, {{ cart.item_count }} {{ cart.item_count | pluralize: 'item', 'items' }}" data-cart-count-wrap>
        {% render 'icon', icon: 'cart' %}
        <span>Cart (<span data-cart-count>{{ cart.item_count }}</span>)</span>
      </a>
    </div>
  </div>
</header>

<style>
  .header .logo--img img {
    height: {{ logo_height }}px;
    width: auto;
    display: block;
    {%- if settings.logo_invert or s.logo_invert -%}
    filter: invert(1);
    {%- endif -%}
  }
  @media screen and (max-width: 749px) {
    .header .logo--img img {
      height: {{ logo_height_mobile }}px;
    }
  }
</style>
{% schema %}
{
  "name": "Header",
  "tag": "section",
  "class": "section-header",
  "settings": [
    {
      "type": "checkbox",
      "id": "show_search",
      "label": "Show search icon",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_account",
      "label": "Show account icon",
      "default": true
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Override logo image (leave blank to use theme setting)"
    },
    {
      "type": "range",
      "id": "logo_height",
      "label": "Override logo height (px)",
      "min": 24,
      "max": 80,
      "step": 2,
      "default": 52
    },
    {
      "type": "range",
      "id": "logo_height_mobile",
      "label": "Override mobile logo height (px)",
      "min": 20,
      "max": 60,
      "step": 2,
      "default": 36
    },
    {
      "type": "checkbox",
      "id": "logo_invert",
      "label": "Invert this header's logo",
      "default": false
    },
    {
      "type": "header",
      "content": "Sticky behavior"
    },
    {
      "type": "checkbox",
      "id": "sticky_header",
      "label": "Sticky header",
      "default": true
    },
    {
      "type": "select",
      "id": "color_scheme",
      "label": "Color scheme",
      "options": [
        { "value": "default", "label": "Default (light)" },
        { "value": "inverse", "label": "Inverse (dark)" }
      ],
      "default": "default"
    }
  ],
  "blocks": [
    {
      "type": "menu",
      "name": "Menu",
      "settings": [
        {
          "type": "link_list",
          "id": "menu",
          "label": "Menu"
        },
        {
          "type": "checkbox",
          "id": "accent",
          "label": "Highlight links (yellow background)",
          "default": false,
          "info": "Applies a yellow background to every link in this menu."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Header",
      "blocks": [
        {
          "type": "menu",
          "settings": {
            "menu": "main-menu",
            "style": "inline"
          }
        }
      ]
    }
  ]
}
{% endschema %}
